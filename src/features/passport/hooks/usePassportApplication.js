export function usePassportApplication() {
  const nextStep = () => {
    //..
  }
  const previousStep = () => {
    //..
  }
  const saveStepData = (step, data) => {
    //..
  }
  const submitApplication = async () => {
    //..
  }

  return {
    nextStep,
    previousStep,
    saveStepData,
    submitApplication,
  }
}
